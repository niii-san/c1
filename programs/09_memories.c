
int main(){



    /*
     * Two types of memory allocation 
     * 1. Static Memory Allocation (SMA)
     * 2. Dynamic Memory Allocation (DMA)
     * */

    /*
     * Static memory allocation
     * in this the memory is fixed it cannot be changed at runtime, the memory is already fixed when compile time
     * so we also call it compile time memory allocation
     * it is automatically freed when it exists function or program ends
     * they are stored in stack/global
     * e.g: int[2], char[2] the size is fixed.*/

    /*
     * Dynamic memory alloation
     * this means we can allocate memory as we need at run time as much as we need its not fixed.
     * we need to manually free memory
     * */



    return 0;
}
